# 更新记录

## v1.12.3

发布日期：2026-09-02

- 恢复站点编辑器中的“播放回源地址（可选）”，新建和编辑站点都可配置独立播放回源，并继续保留主线路、备用线路和自动发现。
- 修复 README 界面预览中的缩进问题，避免 GitHub 将截图表格显示为原始 HTML 文本。

## v1.12.2

发布日期：2026-08-21

- 双向流量计费改为 `入站 + 出站`，每个方向的代理载荷字节只计算一次。
- 配额预留会按实际读写字节结算，短读、短写或 I/O 错误不再消耗未传输的额度。
- 历史自定义趋势区间不再混入当前待落盘流量，实时数据会落入当前时间所在的趋势桶。
- 受信反向代理报告 HTTPS 时，管理面板会正确下发 HSTS；非受信来源无法通过伪造转发头触发。
- Docker Compose 示例在容器内监听桥接网络，同时仍只向宿主机回环地址暴露管理端口。
- 源码构建、Docker 默认构建参数、内嵌前端和文档版本统一为 `v1.12.2`，并增加一致性检查。
- 登录与首次初始化在 1 分钟内连续失败 5 次后立即锁定 60 秒，API 返回 `Retry-After`，页面显示可见倒计时。
- 一键脚本会按发行版补齐基础命令，并在需要验证签名时下载、校验和使用固定版本的临时 cosign。
- 站点线路编辑器拆分 HTTP/HTTPS、地址和端口，HTTPS 新线路默认 443；自动发现界面收敛为 Safe、Compatible、Extreme 三档完整预设。
- Go 可执行程序及测试移入 `cmd/meridian/`，CI、发布、Docker 和源码构建命令同步更新，仓库根目录不再堆放源码文件。
- README 使用全新零站点数据库重新截图，空实例仪表盘会正确显示数值 `0` 而不是占位符。

## v1.12.1

发布日期：2026-08-21

- 修复签名 URL 的隐式 HTTPS 默认端口被改写为显式 `:443`，导致上游签名校验失败的问题。
- 修复管理面板默认绑定与实际监听不一致、普通短读写被误判为配额耗尽、SSE 方法约束、JSON 请求边界和 Telegram 凭据请求重定向等审计问题。
- 修复凭据加密密钥与 JWT、动态路由、上游请求头密钥的分离校验，避免对凭据形态的配置值执行额外哈希比较。
- 完成趋势图、动态播放路由、双向流量统计、移动端交互、Cloudflare ACME 文档及依赖更新。

## v1.12.0

- 作为 `v1.12.1` 的发布基线保留；完整历史可通过 Git 标签和提交记录查看。
